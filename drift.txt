Create a complete Jupyter Notebook (.ipynb) for an end-to-end MiniBatchKMeans segmentation pipeline.

Requirements:
- Dataset name: df (assume I already loaded it or I will replace the read_csv path)
- Dataset size: ~10M rows, 60–70 mixed numeric and categorical features
- Steps required in the notebook:
  1. Import all necessary libraries (pandas, numpy, sklearn, matplotlib)
  2. Load dataset into df (with a placeholder path I can change)
  3. Automatically detect numeric and categorical columns
  4. Split categoricals into low-cardinality and high-cardinality
  5. Preprocessing:
        - Numeric: median imputation + StandardScaler
        - Low-card categoricals: most_frequent impute + OneHotEncoder
        - High-card categoricals: frequency encoding
  6. Build a ColumnTransformer
  7. Take a sample (up to 1M rows) for fitting
  8. Apply dimensionality reduction using TruncatedSVD (20–40 components)
  9. Try different k values (4 to 10) for MiniBatchKMeans and compute silhouette scores
 10. Plot silhouette score vs k
 11. Fit final MiniBatchKMeans using the chosen k
 12. Transform FULL dataset → embeddings → predict segment labels
 13. Add SEGMENT_ID column to df
 14. Visualize clusters using 2D PCA plot
 15. Show segment size distribution (bar plot)
 16. Do basic segment profiling (means of numeric features)
 17. Save models (preprocessor, SVD, KMeans) using joblib
 18. Provide a scoring function that assigns segments to new data

Notebook formatting rules:
- Use clear Markdown headings for each step
- Include explanations where useful
- Include all code needed to run end-to-end
- No placeholders except the CSV file path

Generate only the notebook cells (Markdown + Code) in correct order so I can paste directly into Jupyter.