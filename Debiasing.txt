import pandas as pd

def get_target(df):
    # Assure that the DataFrame is sorted in chronological order, if not already
    # df = df.sort_values('date_column')
    
    # Create shifted columns for the next three rows (future periods)
    df['next1'] = df['DFRL_PCT'].shift(-1)
    df['next2'] = df['DFRL_PCT'].shift(-2)
    df['next3'] = df['DFRL_PCT'].shift(-3)
    
    # Compute conditions for the next three values being monotonically increasing or decreasing.
    # Using non-decreasing/increasing comparisons. If you need strict monotonicity, change
    # <= to < and >= to > accordingly.
    inc_cond = (df['next1'] <= df['next2']) & (df['next2'] <= df['next3'])
    dec_cond = (df['next1'] >= df['next2']) & (df['next2'] >= df['next3'])
    
    # Compare current value to the prior value.
    # NOTE: Ensure that both 'DFRL_PCT' (current) and 'PRR_EOM_DFRL_PCT' (prior) columns are numeric.
    cond_inc = (df['DFRL_PCT'] > df['PRR_EOM_DFRL_PCT']) & inc_cond
    cond_dec = (df['DFRL_PCT'] < df['PRR_EOM_DFRL_PCT']) & dec_cond
    
    # Set the TARGET column based on the conditions
    df['TARGET'] = 0  # default target is 0
    df.loc[cond_inc, 'TARGET'] = 1
    df.loc[cond_dec, 'TARGET'] = -1
    
    # Clean up the helper columns if you no longer need them
    df.drop(columns=['next1', 'next2', 'next3'], inplace=True)
    
    return df

# Apply the logic group-wise via groupby so each group's ordering is maintained
data_s = data_s.groupby('PRTCP_ID', group_keys=False).apply(get_target)